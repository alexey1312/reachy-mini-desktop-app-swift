# Swift / SPM
.build/
.swiftpm/
Package.resolved.orig

# Xcode / Tuist
*.xcodeproj
*.xcworkspace
Apps/Derived/
DerivedData/
xcuserdata/

# Regenerated by `mise run storybook` from the previews
Apps/ReachyStorybook/Generated/

# Coverage
*.profraw
*.profdata

# Local Claude Code state. `rules/` stays tracked for target-specific instructions.
.claude/*
!.claude/rules/

# Written by the repowise MCP server, per worktree. The index is 34 MB of
# regenerable caches (`wiki.db` alone is 24 MB) and `.vscode/mcp.json` hardcodes
# the absolute path of the worktree it was written in, so neither travels.
.repowise/
.vscode/

# Environment
.envrc
mise.local.toml
.venv-sim/
.venv-sim-placo/
CLAUDE.local.md

# macOS
.DS_Store
