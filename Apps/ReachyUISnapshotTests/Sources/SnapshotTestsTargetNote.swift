// The tests in this bundle are not written by hand: `PrefireTestsPlugin` reads the previews in
// `Sources/ReachyUI/Previews`, generates one XCTestCase per preview file into DerivedData, and
// Xcode compiles them into this target. Reference images land in `__Snapshots__` beside this
// directory. This file only keeps the target's source glob non-empty.
